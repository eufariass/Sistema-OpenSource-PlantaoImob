{
  "name": "imob-opensource",
  "version": "1.0.0",
  "description": "Sistema de Plantão - Imob OpenSource",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "verify-db": "node scripts/verify-db-smoke.js",
    "migrate": "node scripts/run-migrations.js",
    "lint": "node --check server.js && node --check db.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "pg": "^8.20.0",
    "ws": "^8.16.0"
  }
}
